<div class="container">
  <h1>Stupid Coaching</h1>

      <p>
        <% if @question.blank? %>
          😶
        <% else %>
          <%= @question %>
        <% end %>
      </p>
      <p class="coach">Your coach:</p>
      <p><%= @answer %></p>

      <a class="btn btn-danger" href="<%= ask_path %>">Ask a new question</a>
</div>
